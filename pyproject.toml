[project]
name = "plugagent"
version = "0.3.0"
description = "PlugAgent — a personal agent that learns and records, as a Claude Code plugin"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]
